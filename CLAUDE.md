# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

See @AGENTS.md for full project context: what Octo+ Data Portal is, the
target architecture, locked decisions/non-goals, and implementation
gotchas (encoding, CSV edge cases, filename prefixes, dependency ordering).

## Status

No application code exists yet — this repo currently holds only planning
docs (`docs/project-plan.md`, `docs/development/todo.md`). The project is in
Phase 0 (discovery), and the API stack (Node/NestJS vs Python/FastAPI) has
not been chosen, so there are no build/lint/test/run commands to document
here yet.

Once code lands (Phase 1 vertical slice per `docs/project-plan.md` §7),
update this section with the real setup, build, lint, test, and run
commands, plus any architecture notes that only make sense once the repo
layout exists.
